# Reviewed tenant configuration

`tenant.yaml` and any companion files are the reviewed Auth0 configuration. This directory is
version controlled; the raw export in `infrastructure/auth0/export/` is ignored.

Follow the [Auth0 configuration sync guide](../../docs/auth0.md) to pull dashboard changes,
review them, preview updates, and push the reviewed configuration.
