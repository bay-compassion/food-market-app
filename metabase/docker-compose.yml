# Self-hosted Metabase for ad hoc analytics against this app's database.
# See docs/metabase.md for setup instructions and security notes before connecting a database.
#
# This is optional tooling, separate from the deployed app: Netlify does not build or run
# anything in this directory.

services:
  # Metabase's own storage for dashboards, saved questions, and user accounts. This is NOT the
  # app database — the app's data (guests, sessions, visits) is added afterwards, from inside
  # Metabase, as a connection to the existing Netlify DB / Neon database.
  metabase-db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: metabase
      POSTGRES_USER: metabase
      POSTGRES_PASSWORD: ${METABASE_DB_PASSWORD:?set METABASE_DB_PASSWORD in metabase/.env}
    volumes:
      - metabase-db-data:/var/lib/postgresql/data

  metabase:
    image: metabase/metabase:latest
    restart: unless-stopped
    depends_on:
      - metabase-db
    ports:
      - '3000:3000'
    environment:
      MB_DB_TYPE: postgres
      MB_DB_DBNAME: metabase
      MB_DB_PORT: 5432
      MB_DB_USER: metabase
      MB_DB_PASS: ${METABASE_DB_PASSWORD:?set METABASE_DB_PASSWORD in metabase/.env}
      MB_DB_HOST: metabase-db

  # Optional: exposes Metabase at a public https URL via a Cloudflare Tunnel, so the whole team
  # can reach it without port-forwarding or a static IP. Create the tunnel in the Cloudflare Zero
  # Trust dashboard (Networks → Tunnels), pointing it at http://metabase:3000, then copy its token
  # into CLOUDFLARE_TUNNEL_TOKEN. Not started by default — run with
  # `docker compose --profile tunnel up`.
  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    profiles: [tunnel]
    depends_on:
      - metabase
    command: tunnel run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN:?set CLOUDFLARE_TUNNEL_TOKEN in metabase/.env}

volumes:
  metabase-db-data:
