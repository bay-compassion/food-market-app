# template-repo

Template repo for my typical setup

## Deployment

The Vue app in `packages/web` is ready to deploy on Netlify. Connect this
repository in Netlify and use the settings in `netlify.toml`; Netlify will
install dependencies, run the workspace build, and publish `packages/web/dist`.
